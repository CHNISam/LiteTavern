{
  "name": "litetavern",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @litetavern/web",
    "build": "npm run build --workspaces --if-present",
    "test": "npm-run-all test:workspaces test:deployment",
    "test:workspaces": "npm run test --workspaces --if-present",
    "test:deployment": "node --test scripts/validate-deployment-env.test.mjs scripts/internal-api.test.mjs scripts/relationship-import.test.mjs scripts/responsive-layout.test.mjs",
    "test:branding": "node scripts/verify-branding.mjs",
    "test:release": "node --test scripts/release-policy.test.mjs scripts/verify-deployment.test.mjs scripts/workflow-policy.test.mjs",
    "test:coverage": "npm run test:coverage --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "check": "npm-run-all typecheck lint test test:branding test:release build",
    "verify:deployment": "node scripts/verify-deployment.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@eslint/js": "latest",
    "eslint": "latest",
    "npm-run-all2": "8.0.4",
    "typescript": "latest",
    "typescript-eslint": "latest"
  }
}
