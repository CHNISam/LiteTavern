你指出得对。前一版有两个错误：

1. **页面流程图**漏了“角色聊天页 → 角色档案页”，还把启动、身份创建、进入方式等非页面节点混了进去。
2. **交互流程图**只画了聊天生成链路，没有把点击头像进入档案、管理记忆、导入角色卡、选择模型和配置 API Key 的交互补全。

页面流程图应聚焦页面及其流转关系；交互流程则要表达用户操作、系统响应和条件分支。复杂流程适合按具体任务拆开，而不是强行塞进一张超长图。([UXPin][1])

## 一、页面流程图

节点只保留真实页面，不放“启动应用”“游客体验”“进入方式”等过程节点。

```mermaid
flowchart TD
    A[产品介绍页]
    B[登录页]
    C[角色手机·联系人列表页]
    D[角色聊天页]
    E[角色档案页]
    F[角色记忆页]
    G[角色设置页]
    H[模型选择页]
    I[账号与个人设置页]
    J[模型服务配置页]

    A --> B
    A --> C
    B --> C

    C --> D
    D --> C

    D --> E
    E --> D

    E --> F
    F --> E

    E --> G
    G --> E

    G --> H
    H --> G

    C --> I
    I --> C

    I --> J
    J --> I

    H --> J
    J --> H
```

对应的页面层级是：

```text
产品介绍页
├── 登录页
└── 角色手机·联系人列表页
    ├── 角色聊天页
    │   └── 角色档案页
    │       ├── 角色记忆页
    │       └── 角色设置页
    │           └── 模型选择页
    └── 账号与个人设置页
        └── 模型服务配置页
```

---

## 二、交互流程图 1：进入产品并打开角色会话

```mermaid
flowchart TD
    A([用户打开产品])
    B[展示产品介绍页]
    C{用户选择进入方式}

    D[获取或创建匿名用户身份]
    E[进入登录页]
    F[用户提交登录信息]
    G{登录是否成功}
    H[提示登录失败]

    I[加载联系人及会话摘要]
    J[展示联系人列表页]
    K[用户点击联系人]

    L[加载该角色的角色配置]
    M[加载该角色的聊天记录]
    N[加载该角色与当前用户的记忆]
    O{是否存在历史聊天}

    P[展示角色开场消息]
    Q[恢复该角色的历史会话]
    R[进入角色聊天页]

    A --> B --> C

    C -->|游客体验| D --> I
    C -->|账号登录| E --> F --> G

    G -->|否| H --> E
    G -->|是| I

    I --> J --> K
    K --> L --> M --> N --> O

    O -->|否| P --> R
    O -->|是| Q --> R
```

---

## 三、交互流程图 2：角色聊天、模型调用与记忆更新

```mermaid
flowchart TD
    A([用户位于角色聊天页])
    B[用户输入文本或选择回复]
    C[校验并保存用户消息]

    D[读取当前角色绑定的模型配置]
    E{模型当前是否可调用}

    F[提示配置模型服务]
    G[进入模型服务配置页]
    H[用户填写服务商、接口地址和 API Key]
    I[验证模型配置]
    J{验证是否成功}
    K[提示配置错误]
    L[保存模型服务配置]
    M[为当前角色选择模型]
    N[返回角色聊天页]

    O[召回该角色的相关用户记忆]
    P["组装回复上下文<br/>角色卡配置<br/>最近对话<br/>相关记忆<br/>当前输入"]
    Q[调用模型生成角色回复]
    R{模型调用是否成功}
    S[展示失败提示并允许重试]

    T[保存并展示角色回复]
    U{是否产生长期有效信息}
    V[生成或更新该角色的用户记忆]
    W{关系摘要是否需要更新}
    X[更新角色与用户的关系摘要]
    Y[更新联系人列表的消息摘要和时间]
    Z[等待用户下一步操作]

    A --> B --> C --> D --> E

    E -->|否| F --> G --> H --> I --> J
    J -->|否| K --> H
    J -->|是| L --> M --> N --> D

    E -->|是| O --> P --> Q --> R
    R -->|否| S --> Z
    R -->|是| T --> U

    U -->|是| V --> W
    U -->|否| W

    W -->|是| X --> Y
    W -->|否| Y

    Y --> Z
    Z -->|继续聊天| B
```

这里的模型配置规则是：

```text
平台体验额度可用
→ 直接调用平台模型

平台额度不可用或用户主动选择 BYOK
→ 使用用户配置的 API Key

当前角色
→ 只绑定一个已配置模型

API Key
→ 在全局模型服务配置页统一管理
```

---

## 四、交互流程图 3：从聊天进入角色档案

```mermaid
flowchart TD
    A([用户位于角色聊天页])
    B[用户点击角色头像或名称]
    C[进入角色档案页]

    D["展示角色身份<br/>核心人格摘要<br/>关系摘要<br/>共同经历摘要"]

    E{用户选择操作}

    F[返回角色聊天页]

    G[点击记忆入口]
    H[进入角色记忆页]
    I[加载该角色的用户记忆和共同经历]
    J[展示角色记忆列表]
    K{用户是否删除记忆}
    L[用户选择指定记忆]
    M[二次确认删除]
    N{是否确认}
    O[删除指定角色记忆]
    P[刷新角色记忆列表]

    Q[点击角色设置入口]
    R[进入角色设置页]
    S{用户选择设置项}

    A --> B --> C --> D --> E

    E -->|返回聊天| F
    E -->|查看记忆| G --> H --> I --> J --> K
    E -->|角色设置| Q --> R --> S

    K -->|否并返回| C
    K -->|删除记忆| L --> M --> N
    N -->|否| J
    N -->|是| O --> P --> J
```

---

## 五、交互流程图 4：角色卡与模型设置

```mermaid
flowchart TD
    A([用户位于角色设置页])
    B{用户选择设置项}

    C[选择导入角色卡]
    D[打开本地文件选择器]
    E[用户选择角色卡文件]
    F[解析并校验角色卡]
    G{角色卡是否合法}
    H[提示文件或格式错误]
    I["展示导入预览<br/>名称、头像、简介、人格、开场消息"]
    J{用户是否确认导入}
    K[创建或更新角色配置]
    L[返回角色设置页]

    M[选择导出角色卡]
    N[生成角色卡文件]
    O[用户保存角色卡]

    P[选择模型]
    Q[进入模型选择页]
    R[加载已经配置的可用模型]
    S{是否存在可用模型}

    T[用户选择模型]
    U[将模型绑定到当前角色]
    V[返回角色设置页]

    W[进入模型服务配置页]
    X[用户新增或编辑模型服务]
    Y["填写服务商<br/>接口地址<br/>API Key"]
    Z[验证模型服务]
    AA{验证是否成功}
    AB[提示验证失败]
    AC[保存模型服务]
    AD[返回模型选择页]

    B -->|导入角色卡| C --> D --> E --> F --> G
    G -->|否| H --> D
    G -->|是| I --> J
    J -->|否| L
    J -->|是| K --> L

    B -->|导出角色卡| M --> N --> O --> L

    B -->|模型选择| P --> Q --> R --> S
    S -->|是| T --> U --> V
    S -->|否| W --> X --> Y --> Z --> AA

    AA -->|否| AB --> Y
    AA -->|是| AC --> AD --> R
```

## 原型图核对标准

按照这套流程，原型图必须满足：

```text
联系人列表页
└── 点击联系人进入角色聊天页

角色聊天页
├── 点击角色头像或名称进入角色档案页
└── 不直接常驻展示记忆、角色卡或 API Key 设置

角色档案页
├── 展示角色身份、人格摘要、关系摘要、共同经历摘要
├── 进入角色记忆页
└── 进入角色设置页

角色记忆页
└── 只展示当前角色对当前用户的记忆与共同经历

角色设置页
├── 导入角色卡
├── 导出角色卡
└── 进入模型选择页

模型选择页
├── 选择当前角色使用的模型
└── 没有可用模型时进入模型服务配置页

模型服务配置页
└── 配置服务商、接口地址和 API Key
```

[1]: https://www.uxpin.com/studio/blog/creating-perfect-user-flows-for-smooth-ux/?utm_source=chatgpt.com "Creating Perfect User Flows for Smooth UX"
